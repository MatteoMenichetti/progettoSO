#include <stdio.h>

int main() {
    printf("Hello, World!\n");

    /*
     * avvio processi
     * P1, P2, P3
     * exec(binp, 1);
     * decision_function
     * input_manager
     * failure_manager
     */
    return 0;
}
